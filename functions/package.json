{
  "name": "swift-chat-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "echo \"No lint configured\"",
    "serve": "firebase emulators:start --only functions --config ../firebase.functions.json --project swift-400b0",
    "deploy": "firebase deploy --only functions --config ../firebase.functions.json --project swift-400b0",
    "logs": "firebase functions:log --project swift-400b0"
  },
  "dependencies": {
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.2.2"
  }
}
